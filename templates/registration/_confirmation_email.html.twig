<h1>{{ 'Hi! Please confirm your email!'|trans([], 'security') }}</h1>

<p>
    {{ 'Please confirm your email address by clicking the following link'|trans([], 'security') }}: <br><br>
    <a href="{{ signedUrl }}">{{ 'Confirm my Email'|trans([], 'security') }}</a>.
    {{ 'This link will expire in'|trans([], 'security') }} {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}.
</p>

<p>
    {{ 'Cheers!'|trans([], 'security') }}
</p>
